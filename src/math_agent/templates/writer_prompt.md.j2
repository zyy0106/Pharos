# 数学建模国赛论文撰写任务

## ⚠️ IRON RULES（违反任一条即视为失败）

1. **禁编造数据**：所有数字必须可在下文「代码运行结果」或「敏感性结果」中追溯。无来源的数字一律改为定性表述。
2. **禁占位**：不得出现「待补」「略」「TODO」「(数据)」「(略)」之类的占位符。
3. **每段必有论点**：禁止"罗列定义但不论证"，禁止"复述题目"。
4. **LaTeX 兼容**：本论文最终会被 xelatex 渲染为 PDF。三条硬规则：
   - **数学符号必须 `$...$` 包裹**——希腊字母（`σ`, `α`）、下标变量（`s_i`, `\hat{d}_i`）、关系符（`≥`, `≤`）、求和（`∑`）等都要写成 `$\sigma_d$`、`$\alpha_i$`、`$s_i^0$`、`$\sum$`。**裸用 `σ_d` 或 `s_i^0` 会让 PDF 编译失败**。
   - `model_section` 和 `solution` 内允许用 `##` 子标题，其余字段不用 Markdown 标题；禁止顶层 `#`。
   - **不要在正文裸用 `%`、`#`、`&`**——百分号写 `\%`、合并使用 `\&`。
5. **公式独占一行**：关键方程必须用 `\[ ... \]` 包裹独占一行渲染，**不要混在段落中**。例如：
   - ❌ `调度后存量 $\eta_i = s_i + x_i - y_i + \xi_i$，约束包括...`
   - ✅
     ```
     调度后存量满足：
     \[ \eta_i = s_i + x_i - y_i + \xi_i, \quad i = 1, \ldots, n \]
     其中 $s_i$ 表示初始存量，$\xi_i$ 是 STGNN 预测的净流入量。
     ```
   多步推导/带条件的方程，每个步骤一行；行内简单引用（如 `$x_i$`、`$\alpha$`）继续用 `$...$`。
6. **结构化分段**：
   - 每条**假设**单独一段，用 `**假设1**：陈述` + 换行 + `**依据**：论证` 两段成对，**每对之间空一行**。
   - **列表**用 `- 项目` 写成单独行，每项 1 行，**整个列表前后各空一行**。
   - 章节内**每个子论点**独占一段，段间空行。**绝对不要把 5 条假设挤成一坨连写**。

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**：
{% for i in prior_critic.issues %}
- {{ i.problem }}
{% endfor %}

**suggestions**：
{% for s in prior_critic.suggestions %}
- {{ s }}
{% endfor %}

本次重写时**必须**：
1. 逐条回应上述 issues；不能简单复述上一稿。
2. IRON RULE 1（禁编造）仍然铁律。如果上一轮被指出"数字编造"，本轮要么把数字改成定性描述，要么完全删除该数字。
3. 不要为了"看起来回应了" 而硬塞新数字——没有就写没有。

---

{% endif %}
## 题目
{{ problem }}

## 已确认假设（{{ assumptions | length }} 条）
{% for a in assumptions %}
{{ loop.index }}. **{{ a.statement }}**
   - 依据：{{ a.rationale or "（未填写依据）" }}
{% endfor %}

## 模型演化
{% for m in model_versions %}
### {{ loop.index }}. [{{ m.stage }}] {{ m.description[:80] }}
{{ m.description }}

**方程**：
{% for eq in m.equations %}
- $$ {{ eq }} $$
{% endfor %}

**变量**：
{% for k, v in m.variables.items() %}
- `{{ k }}`：{{ v }}
{% endfor %}

{% if m.notes %}**与上版差异**：{{ m.notes }}{% endif %}
{% endfor %}

## 代码运行结果（按计算任务分组，仅作事实源）
{% for a in code_artifacts %}
### [{{ loop.index }}] {{ a.purpose }} — success={{ a.success }}
```
{{ a.stdout[-3000:] }}
```
{% if a.stderr and not a.success %}**stderr**：`{{ a.stderr[-300:] }}`{% endif %}
{% endfor %}

## 敏感性分析结果
{% if sensitivity_runs %}
{% for r in sensitivity_runs %}
- **参数 `{{ r.parameter }}`**：在取值 {{ r.values }} 下，指标 `{{ r.metric }}` = {{ r.results }}
  - 解读：{{ r.interpretation or "（未提供解读）" }}
{% endfor %}
{% else %}
（暂无敏感性结果——sensitivity 章节应说明「未执行敏感性分析」）
{% endif %}

## 图表（已生成）
{% if figures %}
{% for f in figures %}
- **图 {{ loop.index }}（{{ f.purpose }}）**：{{ f.caption or "（无 caption）" }}
  - 分析：{{ f.analysis or "（无分析）" }}
{% endfor %}
{% else %}
（暂无图表）
{% endif %}

---

## 各章节字数预算与结构要求

| 章节 | 字数区间 | 强制结构 |
|---|---|---|
| `abstract` | 400–600 | 4 个要素：问题—方法—**关键数字结果**—创新点；至少出现 1 个具体数字 |
| `problem_restatement` | 800–1200 | 背景与实际意义 + 题目条件提炼 + 明示「决策变量 / 目标 / 约束」三要素 + 核心问题分析 |
| `assumptions` | 5–10 条必要假设 | 每条形如「**假设N**：陈述」+「**依据**：……」+「**失效影响**：……」 |
| `notation` | 完整表格 + 说明 | `\| 符号 \| 含义 \| 单位 \| 类型 \|`，覆盖前述所有 `m.variables`，表格后附符号约定说明 |
| `model_section` | 2500–4500 | 按赛题小问组织最终模型；含模型动机、方程、参数、约束与回答指标 |
| `solution` | 1800–3500 | 可复现算法 + 逐问结果 + 对照、稳健性与误差分析 |
| `sensitivity` | 1000–1800 | 分析方法说明 + 参数敏感度排序 + ≥2 条结论性断言 + 管理建议 |
| `conclusion` | 800–1200 | 四段式：优点(≥3) / 缺点与局限(≥2) / 推广方向(≥2) / 方法论总结 |
| `references` | ≥5 条 | `[n] 作者. 标题. 出处, 年.` 格式 |

## 中文学术写作守则（违反将影响"写作清晰"评分）

**禁用词替换表**（出现请改写）：
- 深入探讨 / 深入研究 → 研究 / 分析 / 考察
- 至关重要 / 举足轻重 → 关键 / 重要
- 不难看出 / 显而易见 / 众所周知 → 删除整句开头，直接陈述事实
- 在某种意义上 / 在某种程度上 → 删除或具体化
- 为...提供了新的思路 → 用具体改进点替代

**段首禁忌**：禁用「众所周知」「显而易见」「综上所述」开头（结论段允许一次「综上」）

**标点节律**：
- 破折号 `——` 全文 ≤2 次
- 分号 `；` ≤2 个/千字（优先用句号）
- 禁止连续 ≥3 段都以"冒号+列表"开头

**段落多样性**：禁止 ≥5 段连续相同句数；用 2–8 句变化的段长承载不同功能（短段=强调，长段=论证）

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "abstract": str,
  "problem_restatement": str,
  "assumptions": str,
  "notation": str,
  "model_section": str,
  "solution": str,
  "sensitivity": str,
  "conclusion": str,
  "references": str,
  "keywords": str
}
```

`keywords`：5 个具体关键词，逗号分隔，例如 `"混合整数规划, 时空图神经网络, 条件风险价值, 敏感性分析, 共享单车"`。**禁止**写成空泛词如"数学建模/优化/多智能体"。

生成前，请在思考中先完成 5 维自检（不输出）：
- 假设合理性：每条假设是否在 `assumptions` 章节被显式承接？
- 模型创新：`model_section` 是否突出了相对经典做法的改进点？
- 结果正确：`solution` / `sensitivity` 中的数字是否能在「代码运行结果」中追溯？
- 写作清晰：是否触发了上文「禁用词」「段首禁忌」？
- 综合深度：`sensitivity` 与 `conclusion` 是否给出了实质洞察而非套话？

若某一维不达标，请在生成对应章节时返工，**不要**在 JSON 里夹带自检结果。
