# 章节撰写：模型的求解

本轮你**只**撰写一个字段：`solution`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
{{ outline_solution }}

---

## 题目
{{ problem }}

## 模型方程（用于求解引用）
{% for m in model_versions %}
{% for eq in m.equations %}
- \[ {{ eq }} \]
{% endfor %}
{% endfor %}

## 代码运行结果（仅作事实源，正文不得复写英文协议标签）
{% for a in code_artifacts %}
### [{{ loop.index }}] {{ a.purpose }} — success={{ a.success }}
```
{{ a.stdout }}
```
{% if a.stderr and not a.success %}**stderr**：`{{ a.stderr[-300:] }}`{% endif %}
{% endfor %}

## 正式主方案代码（算法名称与步骤以此为准）
{% for a in code_artifacts %}
{% if a.evidence_role == "primary" %}
```python
{{ a.code }}
```
{% endif %}
{% endfor %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `solution` | 1800–3500 | 以“算法可复现—结果逐问作答—对照与检验”为主线。篇幅来自有效计算证据，不重复模型定义或堆叠运行流程。 |

### 三段结构（用 `##` 标题）

**## 求解算法与流程**
- 算法名称必须与正式主方案代码一致；禁止写入未执行的算法
- 给出可复现伪代码级步骤、关键参数、停止条件、复杂度和随机种子
- 关键步骤的公式代入（把通用方程填入题目具体参数）
- 收敛性/最优性保证（若有）

**## 各问题求解结果**
- 按 Blueprint 小问逐项给出方案、关键数值和现实含义
- 对每个核心结论采用“结论—数值证据—机制解释—适用边界”
- 软约束违约、失败事件或更差基线必须如实保留，不能只展示有利指标

**## 对照、稳健性与误差分析**
- 实验环境（数据规模 / 时间窗口）
- 主方案加至少 2 个独立有效基线的描述与设置理由
- 各方案定量结果（从上方「代码运行结果」提取，必须与原始数值输出一致）
- 结果分析：每个结论用「结论→依据→分析」结构，依据来自已给出的计算结果
- 分别解释实际执行的局部搜索、随机稳健性、服务诊断和动态实验；不得把固定路线蒙特卡洛评价写成随机规划已经求解

**IRON RULE 1（禁编造）铁律**：solution 中的所有数字必须可在上方「代码运行结果」中追溯。无来源的数字一律改为定性表述。

**单位口径**：`avg_delivery_time` 由源码中的分钟制时间递推得到，单位为分钟；`response_time` 单位为秒，扩展实验中的 `runtime_ms` 单位为毫秒。`vehicles` 表示车辆/路线数，不是客户点数或配送点数。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "solution": str
}
```
