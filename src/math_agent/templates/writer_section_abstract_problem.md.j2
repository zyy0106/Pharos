# 章节撰写：摘要 + 问题重述 + 关键词

本轮你**只**撰写三个字段：`abstract`、`problem_restatement`、`keywords`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
- 摘要锚点：{{ outline_abstract }}
- 问题重述锚点：{{ outline_problem_restatement }}

---

## 题目
{{ problem }}

## 已确认假设（{{ assumptions | length }} 条，此处仅供摘要引用，详细论证见假设章节）
{% for a in assumptions %}
- {{ loop.index }}. {{ a.statement }}
{% endfor %}

## 模型演化（概览）
{% for m in model_versions %}
- [{{ m.stage }}] {{ m.description[:80] }}
{% endfor %}

## 代码运行结果（末尾摘要）
{% for a in code_artifacts %}
- [{{ loop.index }}] {{ a.purpose }} — success={{ a.success }}
```
{{ a.stdout }}
```
{% endfor %}

## 图表数量
共 {{ figures | length }} 张图。

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 各字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `abstract` | 350–500 | 4 个要素：问题—方法—**关键数字结果**—创新点；除主方案成本、服务率和碳排放外，至少保留 2-opt 改进率、蒙特卡洛尾部指标或动态回退率中的两个，避免罗列全部指标 |
| `problem_restatement` | 800–1500 | 用一页左右完成背景压缩、逐问数学化、关键难点与总体路线。数据清洗细节只保留会改变模型口径或结果可信度的部分。 |
| `keywords` | 5 个关键词 | 逗号分隔，具体不空泛 |

摘要和问题重述中的算法必须写成“容量拆分与逐步最小增量构造启发式 + 路线内 2-opt + 事件驱动局部重插”。必须区分确定性排线、固定路线蒙特卡洛评价和五类动态事件实验。禁止写遗传算法、ALNS、STGNN、XGBoost、动态规划或精确求解器已经执行。`vehicles` 表示车辆/路线数，`avg_delivery_time` 单位为分钟，`response_time` 单位为秒。

`keywords`：5 个具体关键词，逗号分隔，例如 `"绿色物流, 分割配送, 时变车辆路径, 软时间窗, 敏感性分析"`。**禁止**写成空泛词如"数学建模/优化/多智能体"。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "abstract": str,
  "problem_restatement": str,
  "keywords": str
}
```
