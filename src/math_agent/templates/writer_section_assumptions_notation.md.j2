# 章节撰写：模型假设 + 符号说明

本轮你**只**撰写两个字段：`assumptions`、`notation`。其余章节由其他轮次处理，不要输出。

{% include 'writer_iron_rules.md.j2' %}

---

## 本段大纲锚点
- 假设锚点：{{ outline_assumptions }}
- 符号说明锚点：{{ outline_notation }}

---

## 题目
{{ problem }}

## 已确认假设（{{ assumptions | length }} 条）
{% for a in assumptions %}
{{ loop.index }}. **{{ a.statement }}**
   - 依据：{{ a.rationale or "（未填写依据）" }}
{% endfor %}

## 模型变量（来自 model_versions，符号说明须覆盖全部）
{% for m in model_versions %}
{% for k, v in m.variables.items() %}
- `{{ k }}`：{{ v }}
{% endfor %}
{% endfor %}

---

{% if prior_critic %}
## 上一轮 PaperCritic 反馈（必须逐条回应）

PaperCritic 评分：{{ prior_critic.score }}/10，approved={{ prior_critic.approved }}。

**issues**（仅与本段相关，其余可忽略）：
{% for i in prior_critic.issues %}
- [{{ i.section }}] {{ i.problem }}
{% endfor %}

本次重写时**必须**逐条回应上述与本段相关的 issues；不能简单复述上一稿。
{% endif %}

---

## 各字段字数预算与结构要求

| 字段 | 字数区间 | 强制结构 |
|---|---|---|
| `assumptions` | 800–1600，通常 5–10 条 | 只保留不可由题面直接读取、且会影响模型或结论的必要假设。每条包含陈述、依据和失效影响，不为凑数拆分同义假设。 |
| `notation` | 500–1000 | 表格覆盖正文高频核心变量、单位和类型；一次性中间量在公式邻近处定义，不把所有程序字段堆进符号表。 |

假设必须结构化分段：每条假设单独一段，`**假设N**：陈述` + 换行 + `**依据**：论证` + 换行 + `**合理性分析**：该假设的影响与放宽方向`，**每对之间空一行**。不得将多条假设挤成一坨连写。

**本题口径覆盖（优先级高于上方旧假设措辞）**：
- 附件存在超过单车载重或容积的需求，因此采用 split-delivery：普通订单尽量保持整体，超容量订单/客户聚合需求允许拆为多个单车可行访问任务。禁止写“所有订单绝不拆分”。
- 每次客户访问的统一服务时间为20分钟；坐标是 `X (km), Y (km)` 平面公里坐标，使用欧氏距离。
- 动态数值实验只验证订单取消后的局部重新插入，不得声称四类事件都已完成数值验证。
- 构造启发式给出可行上界，不宣称获得混合整数规划全局最优解或最优性间隙。

{% if retrieved_context %}

---

## 检索到的参考资料（仅供启发，不可照抄）
{{ retrieved_context }}
{% endif %}

---

## 输出契约

请返回严格符合下列 schema 的 JSON（**不得包含其他键**）：

```json
{
  "assumptions": str,
  "notation": str
}
```
