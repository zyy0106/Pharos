## ⚠️ IRON RULES（违反任一条即视为失败）

1. **禁编造数据**：所有定量数值（百分比、成本、服务率、误差等）必须可在下文「代码运行结果」或「敏感性结果」中逐字追溯。**逐字追溯 = 正文数值与原始数值输出完全一致**，不得四舍五入、不得外推、不得"近似"。若代码输出 `service_rate=0.950`，正文只能写 `0.950` 或 `95.0%`，不得写 `0.986` 或 `98.6%`。无来源的数字一律改为定性表述（如"显著提升""成本较高"）。
2. **禁占位**：不得出现「待补」「略」「TODO」「(数据)」「(略)」之类的占位符。
3. **每段必有论点**：禁止"罗列定义但不论证"，禁止"复述题目"。
4. **LaTeX 兼容**：本论文最终会被 xelatex 渲染为 PDF。三条硬规则：
   - **数学符号必须 `$...$` 包裹**——希腊字母（`σ`, `α`）、下标变量（`s_i`, `\hat{d}_i`）、关系符（`≥`, `≤`）、求和（`∑`）等都要写成 `$\sigma_d$`、`$\alpha_i$`、`$s_i^0$`、`$\sum$`。**裸用 `σ_d` 或 `s_i^0` 会让 PDF 编译失败**。
   - **章节标题**：`model_section` 和 `solution` 内的子节用 `## xxx` 标题；更深层子节用 `### xxx`。**禁止顶层 `#`**——那会生成与模板 `\section{}` 冲突的 `\section{}`。其他章节（abstract / assumptions / notation / sensitivity / conclusion）不用标题，用加粗段落开头。
   - **不要在正文裸用 `%`、`#`、`&`**——百分号写 `\%`、合并使用 `\&`。
5. **公式独占一行**：关键方程必须用 `\[ ... \]` 包裹独占一行渲染，**不要混在段落中**。例如：
   - ❌ `调度后存量 $\eta_i = s_i + x_i - y_i + \xi_i$，约束包括...`
   - ✅
     ```
     调度后存量满足：
     \[ \eta_i = s_i + x_i - y_i + \xi_i, \quad i = 1, \ldots, n \]
     其中 $s_i$ 表示初始存量，$\xi_i$ 是 STGNN 预测的净流入量。
     ```
   多步推导/带条件的方程，每个步骤一行；行内简单引用（如 `$x_i$`、`$\alpha$`）继续用 `$...$`。
6. **结构化分段**：
   - 每条**假设**单独一段，用 `**假设1**：陈述` + 换行 + `**依据**：论证` 两段成对，**每对之间空一行**。
   - **列表**用 `- 项目` 写成单独行，每项 1 行，**整个列表前后各空一行**。
   - 章节内**每个子论点**独占一段，段间空行。**绝对不要把 5 条假设挤成一坨连写**。

7. **禁泄露**：正文中不得出现 PaperCritic、Claim、Evidence、Reasoning、issue（作为英文词）、checkpoint、stdout、stderr、artifact、attempt、RESULT、writer、runner、supervisor、finalizer、pipeline、门禁、正式主方案、回应、代码[数字]、超时、占位、李华、张三、王五。这些是工程内部流程痕迹或占位符。应改写为“本文方案”“求解程序”“计算结果”“有效性检查”等竞赛论文用语；与研究无关的恢复、节点和写作流程直接删除。
