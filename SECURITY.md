# 安全说明

## API 密钥

- 永远不要提交 `.env`、`runs/` 或包含模型请求的日志。
- 只提交 `.env.example`，真实密钥在新电脑本地填写。
- 如果密钥曾经出现在聊天、截图、日志或 Git 历史中，应立即在服务商后台撤销并重新生成。

## 运行隔离

数学建模代码会在本机子进程中执行。请只运行你理解或审核过的代码，并在 WebUI 的 Supervisor、第一审和第二审全部通过前不要把结果当成最终答案。

## 报告漏洞

请不要公开发布密钥、题目附件或运行产物；报告问题时删除这些内容后再提交 issue。
